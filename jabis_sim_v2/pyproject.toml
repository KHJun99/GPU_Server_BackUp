[project]
name = "jabis-sim-v2"
version = "0.1.0"
description = "v2 RL track for SO-ARM101 cube lift (BC+PPO)"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
