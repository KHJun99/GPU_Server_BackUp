# Session Handoff — 2026-05-18 AM/PM

**작성**: 2026-05-18, ACT v2 (stage 0 90%, stage 1 60%) 달성 후
**이전 핸드오프**: `docs/HANDOFF_2026-05-17.md` (PPO A 야간 학습 시작)

---

## ⏩ 오후 갱신 (v2 결과 + curriculum cfg)

ACT v0 (stage 0 only) 73% 이후:
- v1 시도: stage0+1 merged 359 → **negative transfer** (stage 0 67%, stage 1 26%)
- v2 시도: stage0+1 merged **1059** → **positive transfer** (**stage 0 90%, stage 1 60%**)

핵심 통찰: stage 1 demo 양을 stage 0의 1/3 이상으로 모아야 (비율 2.7:1) 둘 다 향상. 8:1 비율은 stage 0 fit이 dominate해 stage 1 디테일 못 배우고 stage 0도 noisy해짐.

코드 변경:
- `cfg.CurriculumCfg.current_stage_idx`: stage index를 cfg에서 단일 소스로 노출
- `env.py`: `cfg.curriculum.side_length_m[current_stage_idx]` 사용
- `scripts/eval_policy.py`, `scripts/rollout_video.py`, `scripts/collect_demos.py`: `--curriculum-stage` flag
- `scripts/collect_demos.py`: `--max-success` flag (time-bounded collect; stage 1은 ~22s/ep라 1000 attempts ≈ 6h, 700 max-success로 capping)

ckpt 위치:
- v0: `runs/stage0_bc_act_k12/bc.pt`
- v1: `runs/stage01_bc_act_k12/bc.pt` (negative transfer, reference)
- **v2 (best)**: `runs/stage01v2_bc_act_k12/bc.pt`

다음 단계: stage 2 (15cm) cheap eval 진행. 결과 ≥70%면 stage 2 advance, < 70%면 stage 1/2 분포 demo 같이 collect.

---

## 🌙 야간 갱신 (2026-05-18 22:00 → 23:15) — RL 트랙 전환 시도 + 실패 + 큰 발견

### 사용자 redirect (22:00)
> "이미 다른 팀원이 모방학습을 진행하고 결과가 어느정도 나온상탠데 뭐하자는거야?"

CLAUDE.md "팀 프로젝트는 RL을 포기, 본인이 계속 진행" 무시하고 BC 우회 결정한 것이 잘못. RL 트랙으로 전환.

### 팀원 BC 모델 정보 (사용자 제공)
- HuggingFace `ssafy-d101/` 조직, LeRobot ACT (image+state, ResNet-18 + Transformer + CVAE)
- Task = buds_remove / kboard_create 등 (분해/조립), **cube PnP 아님**
- Obs = 3 cam image + 6/12-D state, Action = 6-D absolute joint
- **호환성 0** (task/obs/action/arch 모두 다름) → 직접 통합 불가
- 단, paradigm(BC) 차지 명확 → 우리 RL 트랙 차별화 정당성 강화

### RL 트랙 첫 시도 (A 옵션): PPO + BC v2 pretrain
- BC v2 ckpt (`stage01v2_bc_act_k12`)를 PPO actor 초기화
- 코드 변경: `ppo.py` load_bc에 ChunkedActorCritic 분기 (chunking_enabled 메타 보고)
- `train_ppo.py` `--actor-logstd-override` flag (stochastic noise 축소용)

**시도 1**: logstd=-1.0 기본값 + GPU 1 → lerobot 학습과 경쟁으로 sps 4로 떨어짐 + ep_return=0. Abort.

**시도 2**: logstd=-2.5 (std≈0.082) + GPU 0 단독 → sps 22 복귀했지만 **24 iter 모두 ep_return=0**. Abort.

### 결정적 발견 (C4 가설 검증) — 23:13

`eval_policy.py`에 `--no-chunk-buffer` flag 추가 → BC v2 actor_mean(chunk[0]) 단독 평가.

| BC v2 stage 0 | Success |
|---|---|
| ChunkBuffer ON (기존 측정) | **90%** |
| **ChunkBuffer OFF (actor_mean 단독)** | **5%** |

**진실**: BC v2 90%는 **거의 전부 ChunkBuffer trick 효과** (temporal ensemble α=0.2 + gripper_use_latest). 진짜 actor는 5%. PPO는 actor_mean만 사용 → 시작점 5% → stochastic noise → 0%.

**의미**:
- 우리 BC ACT 작업은 inference trick으로 success rate 부풀려졌음
- ChunkBuffer 인터페이스 없는 학습기(PPO, SAC fine-tune)는 시작점 5% → 실패 확정
- 차별화 가치 평가도 재검토 필요 — 진짜 policy 자체는 약함

---

## 4. 다음 단계 (내일 결정용 정리)

| 카드 | 시간 | valid 여부 |
|---|---|---|
| **1-step BC (no chunking) 새로 학습 → SACfD** | 1-2일 | ✅ 정석 RL+BC hybrid, ChunkBuffer 의존 X |
| **HER + SAC** (Hindsight Experience Replay) | 1-2일 | ✅ sparse env 정공법 |
| **ChunkBuffer-aware PPO 구현** | 1일 + 학습 | ⚠️ 구현 복잡, ACT 효과 보존 |
| **Dense reward 재설계 (phase gate 강화) + 1-step BC + PPO** | 6-8h | ⚠️ 어제 D/A 변형, hacking risk |
| **Pure RL (no BC)** | 1-2일 | ❌ sparse env에서 사실상 불가능 (어제 D=0% 증명) |

**추천 (내일 오전)**: **1-step BC + SACfD**. 차별화 가치 + 정석 RL paradigm + ChunkBuffer 의존 회피.

## 5. 코드 자산 (보존)

| 파일 | 변경 |
|---|---|
| `ppo.py` | `load_bc()`에 ChunkedActorCritic 분기 + chunking 메타 hard assert |
| `train_ppo.py` | `--actor-logstd-override` flag |
| `eval_policy.py` | `--no-chunk-buffer` flag (가설 검증용) |

ckpt:
- `runs/stage01v2_bc_act_k12/bc.pt` — 보존 (CB on 90%, off 5%)
- `runs/stage0_bc_act_k12/bc.pt` — v0, 참고용
- `runs/ppo_bc_v2_pilot50k_gpu0/` — pilot failed, delete OK

---

---

## TL;DR

- 어제 PPO A (BC 3k + bounded·annealed dense shaping, 500k step) → **0/100 실패**. EE-cube 짓누르기 attractor (영상 검증).
- 결정 트리 시나리오 D-2 처방대로 **ACT (Action Chunking) 도입**.
- BC ACT k=12 단독으로 **stage 0 eval 73/100 = 73% (시나리오 A)**. lift/release/place/vel 모두 100%. visual_agreement만 73% (proxy strictness, 정책 결함 X).
- 영상 검증 6 ep: 5 succ, **reward hacking 없음** (짓누르기/굴리기/hover/trivial release 모두 0).
- PPO는 우선순위 낮음 — sparse 환경에서 PPO가 BC weight 못 지킴 전적 확인됨.

---

## 1. 오늘 진행 (5/18)

### A. PPO A 결과 분석 (어제 야간 학습 결과)
- 학습 끝나서 status_report.txt + analyze_phase1_results.py 실행
- iter ~152 (step 313k)에서 dense annealing 0 도달 → ep_return=0.000 collapse
- 영상 (rollout_step_00462848_last.png): EE가 cube 위로 내려와 압박 (D 옵션 짓누르기 attractor 재발)
- 결정 트리 판정: 시나리오 D-2 (A=0% ≈ D=0% → 즉시 ACT 카드)

### B. ACT 구현 (REVISE 권고 반영)
Codex plan-review 받음 → REVISE 5개 항목 모두 반영:
- B (off-by-one): `_ep_spans` per-episode + `valid_chunk_starts` precompute
- E (lag): `inference_alpha=0.2` (0.1보다 빠른 반응), gripper dim은 ensemble 우회 (chunk[0] latest)
- I (meta assert): ckpt에 chunking_enabled/chunk_size/alpha/gripper_use_latest/loss_type 저장, eval 진입 시 hard assert
- A (shape assert): `ChunkedActorCritic.actor_mean` 반환 shape 검증
- G (per-offset loss): `_train_chunked`에서 j=0..k-1 offset별 L1 평균 로그

코드 변경:
- `src/khj_rl/training/bc.py` — `ActionChunkingCfg`, `_train_chunked`, save 메타
- `src/khj_rl/training/demo_buffer.py` — `_ep_spans`, `valid_chunk_starts`, `iter_chunked_minibatches`
- `src/khj_rl/training/network.py` — `ChunkedActorCritic` (기존 `ActorCritic` 보존)
- `src/khj_rl/training/__init__.py` — exports
- `scripts/train_bc.py` — `--chunk-size/--chunk-loss/--inference-alpha/--no-gripper-use-latest`
- `scripts/eval_policy.py` — `ChunkBuffer` 인라인, ckpt 메타 분기 + assert
- `scripts/rollout_video.py` — 신규 (third-person camera rollout + thumbnail 추출)
- `src/khj_rl/envs/cube_lift/scene.py` — goal marker visibility fix (opacity 0.6→1.0, height 2mm→5mm, z 3mm 위로)
- `bc.py` `_json_safe`: dataclass 직렬화 (`ActionChunkingCfg` 저장 사고 fix)

Codex final-review 통과 (PROCEED).

### C. BC ACT 학습 + eval
- 3k success demo (964 + 1913 = 2877 ep / 402,764 step / 371,117 valid chunks)
- 50 epoch, batch 256, lr 3e-4, k=12, L1 loss, ~25분 학습
- mean_loss 0.148 → 0.0313, per-offset j0=0.026 j11=0.041 (head→tail 1.6배만 증가)
- Eval 100ep: **73/100 = 73%**. lift/release/place/vel 모두 100%. visual_agreement 73%.

### D. 영상 검증
- `runs/stage0_bc_act_k12/eval_videos_v2/` 6 episode (5 succ, 1 fail)
- 영상 검증: 진짜 top-down approach → grasp → lift → translate → place → release → retreat
- Reward hacking 패턴 0건
- ep003 fail은 cube가 disc 옆 가장자리에 placed → visual_agreement proxy fail (occlusion/frustum). 실제 PnP는 됨.

---

## 2. 다음 세션 첫 명령

```bash
cd /home/j-k14d101/KHJ_RL

# 현 상태 확인
cat runs/stage0_bc_act_k12/eval_videos_v2/ep000_succ_steps131.mp4   # 영상 확인
git log --oneline -5

# Curriculum stage 1 (10cm cube spawn)으로 advance
# cfg.curriculum.side_length_m은 tuple (0.05, 0.10, 0.15, 0.20)
# cube spawn xy range 결정은 EventTermCfg (env.py에서 cfg.randomization 읽음)
# 자세한 위치는 env.py 또는 randomization config 봐서 stage 1 적용
```

---

## 3. 결정 트리 (다음 단계)

```
ACT BC stage 0 = 73% (시나리오 A 통과)
  │
  └─ Curriculum stage 1 평가 (10cm cube spawn, 같은 BC ACT 정책)
       ├─ ≥ 70% → stage 2 (15cm) advance, 같은 정책
       ├─ 50-70% → stage 1 분포 oracle demo 수집 + BC ACT retrain
       └─ < 50% → DAgger 검토 (docs/dagger_design.md). PPO fine-tune은 후순위
                  (sparse 환경에서 PPO가 BC 망가뜨림 전적)
```

Stage 3 (20cm) 통과 후 Phase 2 (Object 축 확장) 진입.

---

## 4. 알아두면 좋은 것

### 사고 / 새 troubleshooting 후보

| 사고 | 해결 |
|---|---|
| `_json_safe`가 dataclass 처리 안 함 → bc_meta.json mid-write 실패 | `is_dataclass` 분기 + `asdict` 추가 (이번 commit) |
| goal_marker `opacity=0.6 + height=2mm + z=+1mm`가 dome light 600 아래 invisible | opacity 1.0 + height 5mm + z 3mm 위로 (이번 commit) |
| `sim_app.close()` hang — eval/rollout_video 모두에서 재발 | 어제 troubleshooting #17과 동일 패턴. 결과 다 만들어진 후 process kill OK |

### 영상 + 분석 도구

| 도구 | 용도 |
|---|---|
| `scripts/rollout_video.py` | ChunkBuffer-aware 정책 평가 영상 (third-person camera 자동 활성, ACT temporal ensemble 그대로 적용, 영상 + thumbnail 자동) |
| `scripts/analyze_phase1_results.py` | 학습 run-dir 단위 thumbnail + per-episode breakdown (학습 중 video_sampler 영상 분석 전용) |

### Memory 인덱스 갱신
- `project_ppo_a_overnight.md` — chapter close (0% 실패 + ACT로 대체 성공 명시)
- `project_act_73_breakthrough.md` — 신규, ACT 73% + 다음 단계 처방

---

## 5. 안 한 것 (의도)

- **DAgger 구현** — ACT 73% 충분히 강해서 보류. stage 1/2/3 평가에서 막힐 때 발동
- **PPO ACT fine-tune** — 우선순위 낮음. sparse 환경 PPO 위험 (D=0% 전적). visual_agreement 27% 끌어올리려고 risk 감수할 가치 의심
- **train/val split** — Codex G 권고였지만 첫 통과는 split 없이 진행. stage 1/2에서 overfit 의심되면 그때 도입
- **chunk_size sweep (8/12/16)** + **alpha sweep (0.1/0.2/0.3)** — Codex D/E 권고였지만 k=12 + alpha=0.2 단일 config로 73% 도달해 sweep 불필요. stage advance에서 막히면 그때
