# Session Handoff — 2026-05-17 야간 → 2026-05-18 오전

**작성 시점**: 2026-05-17 23:55 KST
**다음 세션 권장 시점**: 2026-05-18 09:00 KST 전후 (PPO A 학습 완료 + watcher 자동 eval 끝난 후)

---

## 1. 현재 살아있는 작업 (tmux 세션, SSH 끊겨도 진행)

| Session | 역할 | 종료 예상 |
|---|---|---|
| `ppo_a` | PPO A 학습 (BC 3k + dense ON, 500k step) | **~06:10** |
| `watcher` | PPO 완료 자동 감지 → eval 100ep → report 생성 | **~06:30** |
| `health` | GPU/disk/tmux 10분마다 모니터 (zombie kill 없음) | 자동 종료 X |

---

## 2. 오늘 (5/17) 진행한 작업 요약

### Phase 1 학습 시도 순서

| 옵션 | 결과 | 상태 |
|---|---|---|
| **D**: PPO hyperparam 보존 (ent_coef=0, critic_warmup=5, lr=5e-5) | iter=27까지 PnP 시도, iter=40부터 짓누르기 attractor | 실패 (BC weight 보존만 됨, 정책은 변형) |
| **B**: demo 3배 (964 → 2877 success) + BC retrain | BC eval 19/100 = 19.0% (이전 15%에서 +4%p) | 부분 효과, 본질 미해결 |
| **A**: bounded·annealed·phase-gated dense shaping | **현재 학습 중**, iter=10, 종료 ~06:10 | 진행 중 |

### 핵심 발견

- BC 19% / lift_history 36% / release_retreat 21% → PnP의 **후반부**(lift→place→release) 단계에서 누적 covariate shift
- D 옵션의 `ent` 안정 하강은 BC 보존이 아니라 **actor가 짓누르기 행동 분포에 정착**한 것 (영상으로 확인)
- A 옵션은 codex 검토 반영해 weight 조정 (`w_lifted_bonus=0.03`, saturate cap, 3중 조건 released_bonus)

### 생성된 문서 / 코드

| 파일 | 목적 |
|---|---|
| `docs/troubleshooting.md` | 19건 사고 정리 (RESOLVED / IN-PROGRESS / RECURRING) |
| `docs/dense_reward_design.md` | A 옵션 상세 설계 + codex 검토 반영 |
| `docs/dense_reward_weight_tuning.md` | A 결과별 weight 조정 matrix (시나리오 A/B/C/D) |
| `docs/action_chunking_design.md` | ACT-style 사전 설계 (A 실패 시 다음 카드) |
| `docs/dagger_design.md` | DAgger 사전 설계 (A + ACT 실패 시 마지막 카드) |
| `docs/phase2_migration_plan.md` | Phase 1 → Phase 2 vec env 이행 계획 |
| `docs/real_robot_deployment.md` | Phase 3 real robot 배포 계획 |
| `scripts/auto_phase1_pipeline.py` | collect → BC → eval → PPO 자동 pipeline |
| `scripts/auto_phase1_watcher.py` | PPO 완료 감지 + 자동 eval + report 생성 (현재 활성) |
| `scripts/analyze_phase1_results.py` | per-episode breakdown + bottleneck + 영상 thumbnail dashboard |
| `scripts/health_monitor.py` | 야간 health 모니터 (zombie kill 글로벌 비활성) |
| `scripts/train_bc.py` | 다중 demo-dir 지원 추가 |
| `scripts/train_ppo.py` | `--dense` CLI 플래그 추가 |
| `src/khj_rl/envs/cube_lift/reward_dense.py` | A 옵션 dense reward 구현 |
| `src/khj_rl/envs/cube_lift/cfg.py` | `DenseRewardCfg` 추가 |
| `src/khj_rl/envs/cube_lift/scene.py` | 빨간 3cm goal marker 추가 (다음 학습/eval부터 영상에 보임) |

### 사고 (모두 RESOLVED)

- demo_buffer `actions` vs `action` 단/복수 mismatch
- scene.py에서 `robot = robot_cfg` 줄 실수로 삭제 → 즉시 복구
- train_bc.py CLI nargs="+" 적용 후 `Path(list)` TypeError → list 처리로 fix
- BC save 시 list[PosixPath] JSON serialize 실패 → `_json_safe` recursive helper 추가
- Isaac Sim sim_app.close() hang (좀비) — eval, collect_demos 두 번 발생 → kill로 처리, watcher가 자동 kill
- health_monitor 첫 launch 시 zombie misclassification으로 PPO A 죽임 → zombie kill 글로벌 비활성화

---

## 3. 09:00 첫 명령

```bash
cd /home/j-k14d101/KHJ_RL

# 1. 학습 + eval 결과 한눈 확인
cat runs/stage0_ppo_a/status_report.txt

# 2. 더 자세한 분석 (영상 thumbnail 포함)
python scripts/analyze_phase1_results.py --run-name stage0_ppo_a
cat runs/stage0_ppo_a/result_analysis.md

# 3. tmux 세션 상태 확인 (이미 다 종료됐을 것)
env LD_LIBRARY_PATH= /usr/bin/tmux ls
```

---

## 4. 결과별 시나리오 분기 (의사결정 트리)

```
PPO A eval 결과 (runs/stage0_ppo_a/eval_result.json)
  │
  ├─ ≥ 70% → 시나리오 A (SUCCESS)
  │    → curriculum stage 1 (cube spawn 5cm → 10cm) advance
  │    → 같은 BC ckpt + dense 그대로, cfg.randomization 수정 후 재학습
  │    → 자세한 절차: docs/phase2_migration_plan.md (Phase 2 준비)
  │
  ├─ 50-70% → 시나리오 B (NEAR PASS)
  │    → 추가 학습 또는 weight 조정 (per-cond 패턴별)
  │    → 처방: docs/dense_reward_weight_tuning.md 시나리오 B 표
  │
  ├─ 30-50% → 시나리오 C (PARTIAL)
  │    → per-condition 회귀 검사 후 weight matrix 적용
  │    → 처방: docs/dense_reward_weight_tuning.md 시나리오 C 표
  │
  └─ < 30% → 시나리오 D (FAIL)
       │
       ├─ lift_history < 29% (BC baseline 36%의 80%)
       │    → dense가 정책 망가뜨림 → ROLLBACK (dense off, sparse 재학습)
       │
       └─ lift_history ≥ 29%
            → A 단독 한계, ACT 도입 검토
            → 처방: docs/action_chunking_design.md
            → ACT도 실패 시: docs/dagger_design.md
```

---

## 5. 새 세션 첫 프롬프트 (copy-paste)

```
어제 야간(2026-05-17 23:05 KST)에 PPO A 학습(BC 3k + dense shaping ON, 500k step)을
시작하고 watcher로 자동 eval까지 걸어두고 자러갔어. 지금 일어났으니 결과 확인 + 다음 단계
진행해줘.

핵심 컨텍스트:
- 어제 D 옵션 실패(짓누르기 attractor) → B 옵션 BC 19% (미미) → A 옵션 dense shaping 발동
- A 옵션 코드 변경: src/khj_rl/envs/cube_lift/{cfg.py, reward_dense.py, env.py}, scripts/train_ppo.py에 --dense
- Codex 검토 반영한 weight: w_ee_cube_dist=0.5, w_lifted_bonus=0.03, w_cube_goal_dist=1.0,
  w_released_bonus=0.5 (3중 조건), dist_cap=0.1m, alpha0=0.01, decay_steps=300k

진행 절차:
1. cat runs/stage0_ppo_a/status_report.txt 실행 — 학습 곡선 + eval 결과 + 자동 next-step 제안
2. python scripts/analyze_phase1_results.py --run-name stage0_ppo_a — 영상 thumbnail + per-episode 분석
3. 결과 시나리오 판단 (docs/HANDOFF_2026-05-17.md 4번 결정 트리 참조)
4. 영상 검증 (reward hacking 없는지 — cube 굴리기, hover, 짓누르기 etc.)
5. 다음 단계 launch 명령 제안

자세한 핸드오프 내용: docs/HANDOFF_2026-05-17.md (오늘 진행한 작업 + 사고 + 코드 변경 +
의사결정 트리 모두 정리됨)

먼저 status_report.txt 확인부터 시작해줘.
```

---

## 6. 주의사항 / 위험 신호

### 정상 신호 (좋음)
- success rate ≥ 30% (이전 PPO D 0%, BC 19%에서 개선)
- lift_history ≥ 36% (BC baseline 유지)
- 학습 곡선에서 `ent` 안정 또는 하강, `ep_return` 음수에서 양수로 상승

### 위험 신호 (영상 검증 필수)
- velocity_stability < 80% → cube 굴리기 의심
- visual_agreement < 50% → 영상 분포 어긋남
- lift_history < 29% → dense가 lift 학습 망가뜨림 (rollback 후보)
- 학습 곡선에서 `ent` 급격 하강 (한 행동에 좁아짐, jabis_sim_v2 hacking 패턴)

### Reward hacking 자동 체크 (영상에서)
- cube 굴리기 / 끌고가기
- Gripper로 cube 짓누르기
- EE를 cube 위에 hover만 함 (lift는 안 함)
- gripper open + 멀리 후퇴만 함 (cube 안 들고)

---

## 7. 백그라운드 작업 정리 명령 (필요 시)

```bash
# 모든 tmux 세션 정리
env LD_LIBRARY_PATH= /usr/bin/tmux kill-session -t ppo_a 2>/dev/null
env LD_LIBRARY_PATH= /usr/bin/tmux kill-session -t watcher 2>/dev/null
env LD_LIBRARY_PATH= /usr/bin/tmux kill-session -t health 2>/dev/null
env LD_LIBRARY_PATH= /usr/bin/tmux kill-session -t collect_extra 2>/dev/null

# Isaac Sim 좀비 프로세스 확인 + 정리
pgrep -af "train_ppo|collect_demos|eval_policy"
# (살아있으면) SIGTERM
```

---

## 8. Memory에 저장됨 (새 세션 자동 로드)

`~/.claude/projects/-home-j-k14d101-KHJ-RL/memory/MEMORY.md` 인덱스에:
- `[PPO A overnight]` (project_ppo_a_overnight.md) — 야간 학습 진행 + 자동화 셋업 요약

새 Claude가 자동으로 이 메모리 + CLAUDE.md (글로벌 + 프로젝트) 로드 → 컨텍스트 즉시 확보.
